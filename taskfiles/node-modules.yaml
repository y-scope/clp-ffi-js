version: "3"

tasks:
  default:
    sources:
      - "{{.ROOT_DIR}}/package.json"
      - "{{.ROOT_DIR}}/package-lock.json"
      - "{{.TASKFILE}}"
    generates:
      - "{{.ROOT_DIR}}/node_modules/**/*"
    run: "once"
    cmds:
      - "npm clean-install"
